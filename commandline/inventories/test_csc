---
all:
  children:
    hpc:
      hosts:
        roihu-cpu-inst.csc.fi:
          module_root: /tmp/{{ ansible_facts['user_id'] }}/ansible/modulefiles
          install_prefix: /tmp/{{ ansible_facts['user_id'] }}/ansible/install
          kielipankki_data_dir: /tmp/{{ ansible_facts['user_id'] }}/ansible/kielipankki_data
          swig3_as_module: no
          shared_group: r_installation_kielipankki
          is_admin: false
  hosts:
    localhost:
      ansible_connection: local
