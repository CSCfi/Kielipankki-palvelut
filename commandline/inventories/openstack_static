# Static IP settings for generated CPouta VMs. Needs to be adjusted if the IP pool changes.
new_openstack_vms:
  hosts:
    new_openstack_vm:
      ansible_host: 195.148.31.151
      ansible_user: cloud-user
      is_admin: true
      swig3_as_module: no
      install_prefix: /appl/ling/