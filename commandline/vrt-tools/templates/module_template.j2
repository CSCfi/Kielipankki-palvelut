help([[
         {{ package_name }} - utilities for processing VRT corpora

This package currently only provides vrt-to-json. See

vrt-to-json --help

for help.
]])

local   version =       '{{ version }}'
local   prog_root =     '{{ install_prefix }}/{{ package_name }}'

prepend_path('PATH', pathJoin(prog_root,version,'bin'))
